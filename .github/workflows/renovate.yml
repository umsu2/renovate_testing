name: Renovate
on:
  # Allows manual trigger from the Actions tab
  workflow_dispatch:
  # Runs at 00:00 every day
  schedule:
    - cron: '0 0 * * *'

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@v41.0.1
        with:
          configurationFile: renovate.json
          token: ${{ secrets.RENOVATE_TOKEN }}
        env:
          # Use the same token for fetching changelogs to avoid rate limits
          GITHUB_COM_TOKEN: ${{ secrets.RENOVATE_TOKEN }}
          RENOVATE_REPOSITORIES: '["${{ github.repository }}"]'
          LOG_LEVEL: "debug"
          RENOVATE_SECRETS: '{"DHI_USERNAME":"${{ secrets.DHI_USERNAME }}","DHI_PASSWORD":"${{ secrets.DHI_PASSWORD }}"}'